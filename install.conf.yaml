- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.zsh:
    ~/.vimrc:
    ~/.vim:
    ~/.gitattributes:
    ~/.gitignore:
    ~/.gitconfig:
    ~/.editorconfig:
    ~/.curlrc:
