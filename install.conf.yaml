- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
      path: zshrc
      force: true
    ~/.zshenv:
    ~/.zsh:
    ~/.vimrc:
    ~/.vim:
    ~/.gitattributes:
    ~/.gitignore:
    ~/.gitconfig:
    ~/.editorconfig:
    ~/.curlrc:
